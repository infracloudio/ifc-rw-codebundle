apiVersion: v1
kind: Service
metadata:
  name: rds-mysql-connection-count-sli-svc
spec:
  selector:
    app: rds-mysql-connection-count-sli
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
